---
import Layout from '../layouts/Layout.astro';

let posts = [];
if (Astro.locals.runtime) {
  const db = Astro.locals.runtime.env.DB;
  const result = await db.prepare('SELECT * FROM posts ORDER BY created_at DESC').all();
  posts = result.results;
} else {
  console.log('Running in dev mode without local runtime');
}

const featuredPost = posts[0];
const restPosts = posts.slice(1);
const hasPosts = posts.length > 0;
const formatDate = (value) =>
  new Date(value).toLocaleDateString('zh-CN', {
    year: 'numeric',
    month: 'long',
    day: 'numeric'
  });
const latestDateLabel = featuredPost ? formatDate(featuredPost.created_at) : '暂无更新';
const primaryLink = hasPosts ? `/posts/${featuredPost.id}` : '/admin';
const primaryLabel = hasPosts ? '阅读最新' : '开始写作';
---

<Layout title="我的博客 - 首页">
  <section class="relative isolate overflow-hidden rounded-[32px] border border-[color:var(--line)] bg-white/70 px-6 py-10 shadow-[0_40px_120px_-80px_rgba(15,118,110,0.55)] sm:px-10 sm:py-14">
    <div class="pointer-events-none absolute -right-20 -top-28 h-64 w-64 rounded-full bg-[radial-gradient(circle_at_top,_rgba(251,191,36,0.45),_transparent_70%)] blur-3xl"></div>
    <div class="pointer-events-none absolute -left-16 -bottom-28 h-64 w-64 rounded-full bg-[radial-gradient(circle_at_center,_rgba(45,212,191,0.35),_transparent_70%)] blur-3xl"></div>
    <div class="relative z-10 grid gap-10 lg:grid-cols-[1.2fr_0.8fr]">
      <div class="space-y-6">
        <p class="anim-fade-up text-xs uppercase tracking-[0.35em] text-[color:var(--muted)]">Paper Notes</p>
        <h1 class="font-display anim-fade-up anim-delay-1 text-4xl leading-[1.05] text-[color:var(--ink)] sm:text-6xl">
          把生活写成可以反复翻阅的章节
        </h1>
        <p class="anim-fade-up anim-delay-2 max-w-xl text-lg text-[color:var(--muted)]">
          记录生活、分享技术、沉淀思考，用更有温度的方式整理成长轨迹。
        </p>
        <div class="anim-fade-up anim-delay-2 flex flex-wrap gap-2">
          <span class="rounded-full border border-[color:var(--line)] bg-white/70 px-3 py-1 text-xs text-[color:var(--muted)]">技术笔记</span>
          <span class="rounded-full border border-[color:var(--line)] bg-white/70 px-3 py-1 text-xs text-[color:var(--muted)]">生活随笔</span>
          <span class="rounded-full border border-[color:var(--line)] bg-white/70 px-3 py-1 text-xs text-[color:var(--muted)]">项目复盘</span>
        </div>
        <div class="anim-fade-up anim-delay-3 flex flex-wrap gap-3">
          <a
            href={primaryLink}
            class="inline-flex items-center gap-2 rounded-full bg-primary-600 px-5 py-2.5 text-sm font-medium text-white shadow-[0_10px_30px_-18px_rgba(13,148,136,0.8)] transition hover:bg-primary-700 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary-500/40"
          >
            {primaryLabel}
            <span aria-hidden="true">↗</span>
          </a>
          <a
            href="/admin"
            class="inline-flex items-center gap-2 rounded-full border border-[color:var(--line)] bg-white/70 px-5 py-2.5 text-sm font-medium text-[color:var(--ink)] transition hover:border-primary-300 hover:text-primary-700 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary-500/40"
          >
            发布文章
          </a>
        </div>
        <div class="flex flex-wrap items-center gap-6 text-sm text-[color:var(--muted)]">
          <span class="flex items-center gap-2">
            <span class="h-2 w-2 rounded-full bg-primary-500"></span>
            最近更新：{latestDateLabel}
          </span>
          <span class="flex items-center gap-2">
            <span class="font-semibold text-[color:var(--ink)]">{posts.length}</span>
            篇文章
          </span>
        </div>
      </div>
      <div class="grid gap-4 anim-fade-in anim-delay-2">
        {featuredPost ? (
          <article class="group flex h-full flex-col justify-between rounded-3xl border border-[color:var(--line)] bg-white/80 p-6 shadow-[0_26px_70px_-50px_rgba(30,18,9,0.55)] backdrop-blur">
            <div class="flex items-center justify-between text-xs uppercase tracking-[0.3em] text-[color:var(--muted)]">
              <span>最新文章</span>
              <span>{formatDate(featuredPost.created_at)}</span>
            </div>
            <div class="mt-5">
              <h2 class="font-display text-2xl text-[color:var(--ink)] transition-colors group-hover:text-primary-700">
                {featuredPost.title}
              </h2>
              <p class="mt-3 text-sm leading-relaxed text-[color:var(--muted)] line-clamp-4">
                {featuredPost.content}
              </p>
            </div>
            <a href={`/posts/${featuredPost.id}`} class="mt-6 inline-flex items-center gap-2 text-sm font-semibold text-primary-700">
              阅读全文
              <span aria-hidden="true">→</span>
            </a>
          </article>
        ) : (
          <div class="flex h-full flex-col items-center justify-center rounded-3xl border border-[color:var(--line)] bg-white/80 p-6 text-center shadow-[0_24px_70px_-50px_rgba(30,18,9,0.45)]">
            <div class="mb-4 inline-flex h-12 w-12 items-center justify-center rounded-full bg-[color:var(--accent-soft)] text-[color:var(--accent)]">
              <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.6" d="M12 6v12m6-6H6" />
              </svg>
            </div>
            <h2 class="font-display text-2xl text-[color:var(--ink)]">写下第一篇</h2>
            <p class="mt-2 text-sm text-[color:var(--muted)]">从一个小故事开始，把这里填满。</p>
            <a
              href="/admin"
              class="mt-6 inline-flex items-center gap-2 rounded-full bg-primary-600 px-5 py-2.5 text-sm font-medium text-white shadow-[0_10px_30px_-18px_rgba(13,148,136,0.8)] transition hover:bg-primary-700 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary-500/40"
            >
              发布文章
            </a>
          </div>
        )}
        <div class="rounded-3xl border border-[color:var(--line)] bg-white/70 p-5 text-sm text-[color:var(--muted)]">
          <p class="text-xs uppercase tracking-[0.3em] text-[color:var(--muted)]">写作节奏</p>
          <p class="mt-3 font-display text-lg text-[color:var(--ink)]">慢一点，也更稳。</p>
          <p class="mt-2">记录技术、生活与思考的交汇点，每周至少一次小结。</p>
        </div>
      </div>
    </div>
  </section>

  <section id="latest" class="mt-14">
    {hasPosts ? (
      <>
        <div class="flex flex-wrap items-end justify-between gap-4">
          <div>
            <p class="text-xs uppercase tracking-[0.35em] text-[color:var(--muted)]">Journal</p>
            <h2 class="font-display text-2xl text-[color:var(--ink)] sm:text-3xl">最新文章</h2>
          </div>
          <p class="text-sm text-[color:var(--muted)]">共 {posts.length} 篇，持续更新中</p>
        </div>
        {restPosts.length > 0 ? (
          <div class="mt-6 grid gap-6 md:grid-cols-2">
            {restPosts.map((post: any) => (
              <a
                href={`/posts/${post.id}`}
                class="group block focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary-500/40"
              >
                <article class="flex h-full flex-col justify-between rounded-3xl border border-[color:var(--line)] bg-white/75 p-6 shadow-[0_24px_60px_-48px_rgba(30,18,9,0.6)] transition-all duration-300 hover:-translate-y-1 hover:shadow-[0_28px_80px_-50px_rgba(15,118,110,0.45)]">
                  <div>
                    <p class="text-xs uppercase tracking-[0.3em] text-[color:var(--muted)]">文章</p>
                    <h3 class="mt-3 font-display text-xl text-[color:var(--ink)] transition-colors group-hover:text-primary-700">
                      {post.title}
                    </h3>
                    <p class="mt-3 text-sm leading-relaxed text-[color:var(--muted)] line-clamp-3">
                      {post.content}
                    </p>
                  </div>
                  <div class="mt-6 flex items-center justify-between text-xs text-[color:var(--muted)]">
                    <time datetime={post.created_at}>{formatDate(post.created_at)}</time>
                    <span class="inline-flex items-center gap-2 text-primary-700">
                      阅读
                      <span aria-hidden="true">→</span>
                    </span>
                  </div>
                </article>
              </a>
            ))}
          </div>
        ) : (
          <div class="mt-6 rounded-2xl border border-dashed border-[color:var(--line)] bg-white/60 px-6 py-8 text-sm text-[color:var(--muted)]">
            更多文章正在路上，先读读上面的最新内容吧。
          </div>
        )}
      </>
    ) : (
      <div class="mt-6 rounded-3xl border border-dashed border-[color:var(--line)] bg-white/60 px-6 py-10 text-center">
        <h3 class="font-display text-2xl text-[color:var(--ink)]">这里还空着</h3>
        <p class="mt-2 text-sm text-[color:var(--muted)]">从一个小故事开始，把你的思考写下来。</p>
        <a
          href="/admin"
          class="mt-6 inline-flex items-center gap-2 rounded-full bg-primary-600 px-5 py-2.5 text-sm font-medium text-white shadow-[0_10px_30px_-18px_rgba(13,148,136,0.8)] transition hover:bg-primary-700 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary-500/40"
        >
          发布文章
        </a>
      </div>
    )}
  </section>
</Layout>
